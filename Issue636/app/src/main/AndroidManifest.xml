<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ibotpeaches.issue636">

    <application android:allowBackup="true" android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher" android:theme="@style/AppTheme">
        <provider android:authorities="@string/authority1" android:exported="false" android:grantUriPermissions="true" android:label="@string/app_name" android:multiprocess="false" android:name="com.ibotpeaches.issue636.Provider"/>
        <provider android:authorities="@string/authority2" android:exported="false" android:grantUriPermissions="true" android:label="@string/app_name" android:multiprocess="false" android:name="com.ibotpeaches.issue636.ProviderTwo"/>
    </application>

</manifest>
